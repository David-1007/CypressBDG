// Convert Minutes into Seconds

function convert(m) {
    const s = 60;
    return (m * s);
}

console.log(convert(3));

// Basic Variable Assignment

let name = "Name";

function concat(m) {
    const surname = "Edabit"
    return (`${name}${surname}`);
}

console.log(concat(name));